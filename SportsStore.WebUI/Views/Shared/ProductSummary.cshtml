@model SportsStore.Domain.Entities.Product

<div class="well">
    <h3>
        <strong>@Model.Name</strong>
        <span class="pull-right label label-primary">@Model.Description</span>
    </h3>
    <span class="lead">@Model.Price.ToString("c")</span>
</div>
